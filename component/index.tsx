import { IProps } from "./props";
import { useStyles } from "./styles";

export function __templateNameToPascalCase__(_: Readonly<IProps>) {
  const { colors, styles, rxStyles } = useStyles();

  return null;
}
