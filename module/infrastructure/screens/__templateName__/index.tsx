import { Props } from "./props";
import { useStyles } from "./styles";

export function __templateNameToPascalCase__(_: Readonly<Props>) {
  const { colors, styles, rxStyles } = useStyles();

  return null;
}
